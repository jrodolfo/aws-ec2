" -------------------------------
" Tiny Vim setup for coding
" -------------------------------

" Enable syntax highlighting
syntax on

" Show line numbers
set number

" Enable auto-indentation
set autoindent
set smartindent

" Tabs as 4 spaces
set tabstop=4
set shiftwidth=4
set expandtab

" Highlight matching brackets
set showmatch

" Enable colors (if terminal supports it)
set termguicolors

" Filetype plugin and indent detection
filetype plugin indent on

" Optional: color scheme (default to desert)
colorscheme desert

